<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
        <item name="android:navigationBarColor">#0E577D</item>
        <item name="android:windowFullscreen">false</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="SplashTheme" parent="android:Theme.Holo.NoActionBar.TranslucentDecor">
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">false</item>
        <item name="android:background">@drawable/bg</item>
        <item name="android:windowFullscreen">false</item>
    </style>
    <style name="FitnessRatingBarMedium" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/review_rating_medium_full</item>
        <item name="android:minHeight">12dp</item>
        <item name="android:maxHeight">12dp</item>
    </style>
    <style name="MerchantRatingBarMedium" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/merchant_rating_medium_full</item>
        <item name="android:minHeight">38dp</item>
        <item name="android:maxHeight">38dp</item>
    </style>
</resources>
